# Quick Plan 260328-n9d

## Goal
Apply a liquid glass style to the right proof block.

## Tasks

1. Add a reusable liquid glass CSS class.
   - Files: `src/styles/globals.css`
   - Action: Define the `liquid-glass-strong` class with gradient background, border, backdrop blur, shadow, and highlight pseudo-element.
   - Verify: The class exists and can be reused on glass-like blocks.
   - Done: Liquid glass styling is available globally.

2. Apply the class to the right proof card.
   - Files: `src/features/landing/proof-section.tsx`
   - Action: Replace the ad hoc translucent wrapper with the reusable glass class and keep content on a foreground layer.
   - Verify: The right proof block visually reads as liquid glass and keeps the headline/button intact.
   - Done: The right proof block uses the liquid glass style.

## Notes

- Keep the image, overlay, and headline layout unchanged.