# Quick Plan 260328-lv6

## Goal
Set the main screen background to `rgba(250, 254, 255, 1)`.

## Tasks

1. Update the first screen and page root background color.
   - Files: `src/features/landing/first-screen.tsx`, `src/app/page.tsx`
   - Action: Replace the white background with `rgba(250, 254, 255, 1)` on the owner layers.
   - Verify: The top screen renders with the requested tint and no layout changes.
   - Done: The main screen background matches the requested color.

## Notes

- Keep the change limited to background color only.