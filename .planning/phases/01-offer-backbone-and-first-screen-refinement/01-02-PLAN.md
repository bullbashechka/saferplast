---
phase: 01-offer-backbone-and-first-screen-refinement
plan: "02"
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - src/features/landing/site-header.tsx
autonomous: true
requirements:
  - SITE-03
must_haves:
  truths:
    - "(SITE-03) Header visibly shows phone, city, WhatsApp, and Telegram in the first screen."
    - "(SITE-03) Header navigation remains usable with only live first-screen destination anchors."
    - "Header styling is mapped from `docs/headerDemoStyles.md` into Tailwind utilities, without raw CSS blocks."
  artifacts:
    - path: "src/features/landing/site-header.tsx"
      provides: "Responsive header layout and contact cluster mapped from demo style values"
      contains: "h-[3rem]"
  key_links:
    - from: "src/features/landing/site-header.tsx"
      to: "docs/headerDemoStyles.md"
      via: "tailwind mapping of dimensions/spacing/typography"
      pattern: "5.75rem|3rem|0.9375rem|1.5rem"
    - from: "src/features/landing/site-header.tsx"
      to: "src/features/landing/first-screen-content.ts"
      via: "props sourced from FirstScreen content contract"
      pattern: "phoneHref|cityLabel|whatsappHref|telegramHref"
---

<objective>
Implement Figma-driven header correction by mapping `docs/headerDemoStyles.md` values to valid Tailwind classes.

Purpose: Deliver an above-the-fold header that communicates navigation + contact/service-area relevance immediately without scope creep.
Output: Updated `site-header.tsx` with concrete Tailwind value mapping and preserved responsive behavior.
</objective>

<execution_context>
@C:/Users/fm/Documents/Business/Saferplast_main/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/fm/Documents/Business/Saferplast_main/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-offer-backbone-and-first-screen-refinement/01-CONTEXT.md
@docs/headerDemoStyles.md
@src/features/landing/site-header.tsx
@src/features/landing/first-screen.tsx
@src/features/landing/first-screen-content.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Map header frame and nav blocks from demo spec to Tailwind values</name>
  <read_first>
    docs/headerDemoStyles.md
    src/features/landing/site-header.tsx
  </read_first>
  <files>src/features/landing/site-header.tsx</files>
  <acceptance_criteria>
    Header wrapper uses Tailwind values equivalent to demo frame dimensions: 92px height and 24px nav-gap (`h-[5.75rem]`, `gap-[1.5rem]` on desktop).
    Nav links use 16px regular text (`text-[1rem] font-normal`) and remain centered on desktop.
    Header implementation does not contain pasted raw CSS declarations such as `position: absolute;` from demo docs.
  </acceptance_criteria>
  <action>Apply concrete Tailwind mapping from `docs/headerDemoStyles.md` in `site-header.tsx`: map header 92px to `h-[5.75rem]`, navigation gap 24px to `gap-[1.5rem]`, and link typography 16px to `text-[1rem] font-normal leading-[1]`. Keep layout responsive with flex wrapping at small breakpoints and centered nav on `lg+`; do not use absolute coordinates from the demo spec, and do not add new header features beyond logo/nav/contact/location blocks per D-04.</action>
  <verify>
    <automated>npm.cmd run lint</automated>
  </verify>
  <done>Header frame and navigation visually match the demo style intent through valid Tailwind utilities.</done>
</task>

<task type="auto">
  <name>Task 2: Map contact and location chip styling with explicit demo values</name>
  <read_first>
    docs/headerDemoStyles.md
    src/features/landing/site-header.tsx
    src/features/landing/first-screen-content.ts
  </read_first>
  <files>src/features/landing/site-header.tsx</files>
  <acceptance_criteria>
    Phone and location chips use mapped values: 48px height (`h-[3rem]`), 15px radius (`rounded-[0.9375rem]`), 30px horizontal padding (`px-[1.875rem]`), and 14px internal gap (`gap-[0.875rem]`) at desktop.
    Phone/location text maps to 20px medium on desktop (`lg:text-[1.25rem] font-medium`) and scales down responsively on smaller screens.
    Location chip uses `#F2F4F5`; phone chip uses `#FAFEFF` (or semantically equivalent Tailwind colors with same hex output).
    Phone, city, WhatsApp, and Telegram remain visible in the same header contact zone.
  </acceptance_criteria>
  <action>Implement contact-block mapping in `site-header.tsx` with explicit target values from `headerDemoStyles.md`: 48px chips (`h-[3rem]`), 15px corner radius (`rounded-[0.9375rem]`), 30px horizontal padding (`px-[1.875rem]`), 14px icon/text gap (`gap-[0.875rem]`), icon sizes 16px phone and 16x20 location. Apply background colors `#FAFEFF` for phone and `#F2F4F5` for location, keep phone as main textual contact, and keep WhatsApp/Telegram as secondary icon links grouped beside phone. Keep everything prop-driven from `FirstScreen`; do not hardcode phone/city/messenger values in the header component and do not modify lower-page sections.</action>
  <verify>
    <automated>npm.cmd run typecheck</automated>
  </verify>
  <done>Header contact cluster satisfies SITE-03 and matches the demo style geometry with explicit Tailwind mappings.</done>
</task>

</tasks>

<verification>
Run `npm.cmd run lint` and `npm.cmd run typecheck`. Confirm `site-header.tsx` contains no raw CSS dump and maps demo values to Tailwind classes with responsive behavior.
</verification>

<success_criteria>
Header is visually corrected to demo style intent, contact/service-area info is immediately visible, and no non-first-screen scope is introduced.
</success_criteria>

<output>
After completion, create `.planning/phases/01-offer-backbone-and-first-screen-refinement/01-02-SUMMARY.md`
</output>