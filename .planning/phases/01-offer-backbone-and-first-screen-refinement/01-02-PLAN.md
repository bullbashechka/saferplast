---
phase: 01-offer-backbone-and-first-screen-refinement
plan: "02"
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - public/icons/whatsapp.svg
  - public/icons/telegram.svg
  - src/features/landing/site-header.tsx
autonomous: true
requirements:
  - SITE-03
must_haves:
  truths:
    - "Visitors can see the phone number and city in the first-screen header."
    - "Visitors can open WhatsApp and Telegram from the same contact cluster without those links overpowering the main CTA."
    - "Header navigation keeps only real destinations available in Phase 1."
  artifacts:
    - path: "public/icons/whatsapp.svg"
      provides: "Local WhatsApp icon asset"
      contains: "<svg"
    - path: "public/icons/telegram.svg"
      provides: "Local Telegram icon asset"
      contains: "<svg"
    - path: "src/features/landing/site-header.tsx"
      provides: "First-screen header with grouped contact actions"
      contains: "whatsappHref"
  key_links:
    - from: "src/features/landing/site-header.tsx"
      to: "src/features/landing/first-screen-content.ts"
      via: "imported contact and navigation data"
      pattern: "firstScreenContent"
    - from: "src/features/landing/site-header.tsx"
      to: "public/icons/whatsapp.svg|public/icons/telegram.svg"
      via: "next/image icon rendering"
      pattern: "whatsapp\\.svg|telegram\\.svg"
---

<objective>
Implement the first-screen header contact cluster and navigation using the shared content contract.

Purpose: Satisfy the visible contact and service-area requirement without bloating the hero or introducing competing contact UI.
Output: Local messenger icons and a responsive `SiteHeader` that shows phone, city, WhatsApp, and Telegram with the correct hierarchy.
</objective>

<execution_context>
@C:/Users/fm/Documents/Business/Saferplast_main/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/fm/Documents/Business/Saferplast_main/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-offer-backbone-and-first-screen-refinement/01-CONTEXT.md
@.planning/phases/01-offer-backbone-and-first-screen-refinement/01-RESEARCH.md
@src/features/landing/site-header.tsx
@src/features/landing/first-screen-content.ts
@public/icons/phone.svg
@public/icons/location.svg

<interfaces>
Expected from src/features/landing/first-screen-content.ts:
```ts
export const firstScreenContent: FirstScreenContent;
// Includes:
// - navigationLinks
// - phoneHref
// - phoneLabel
// - cityLabel
// - whatsappHref
// - telegramHref
```

Current header props:
```tsx
type NavigationLink = {
  href: string;
  label: string;
};

type SiteHeaderProps = {
  navigationLinks: NavigationLink[];
};
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add local messenger icon assets for the header contact group</name>
  <read_first>
    public/icons/phone.svg
    public/icons/location.svg
    .planning/phases/01-offer-backbone-and-first-screen-refinement/01-CONTEXT.md
  </read_first>
  <files>
    public/icons/whatsapp.svg
    public/icons/telegram.svg
  </files>
  <acceptance_criteria>
    `public/icons/whatsapp.svg` exists and contains an `<svg` root.
    `public/icons/telegram.svg` exists and contains an `<svg` root.
    Both assets are simple monochrome or brand-appropriate SVGs sized for header icon use, not raster files or external URLs.
  </acceptance_criteria>
  <action>Create `public/icons/whatsapp.svg` and `public/icons/telegram.svg` as lightweight local SVG assets sized for the same visual scale as the existing phone and location icons. Keep them usable in `next/image` and aligned with D-12: these are supporting contact actions, not hero-level illustrations. Do not install an icon package and do not fetch remote assets.</action>
  <verify>
    <automated>npm.cmd run lint</automated>
  </verify>
  <done>The repo contains local messenger icons ready for the header contact cluster.</done>
</task>

<task type="auto">
  <name>Task 2: Refine SiteHeader to show the approved contact grouping and real nav destinations</name>
  <read_first>
    src/features/landing/site-header.tsx
    src/features/landing/first-screen-content.ts
    .planning/phases/01-offer-backbone-and-first-screen-refinement/01-CONTEXT.md
    .planning/phases/01-offer-backbone-and-first-screen-refinement/01-RESEARCH.md
  </read_first>
  <files>src/features/landing/site-header.tsx</files>
  <acceptance_criteria>
    `src/features/landing/site-header.tsx` imports `firstScreenContent`.
    The phone link renders `firstScreenContent.phoneLabel` with a `tel:` href and remains the main textual contact in the contact zone.
    The file contains both `whatsappHref` and `telegramHref` links grouped beside the phone, and still renders `cityLabel`.
    No `#projects` or `#contacts` strings remain in the header file.
  </acceptance_criteria>
  <action>Refactor `SiteHeader` to consume navigation links and contact values from `firstScreenContent` created in 01-01. Keep the responsive header shell and logo, but update the contact area so D-10 through D-12 are explicit: phone is the dominant text action, city remains visibly present, and WhatsApp/Telegram are separate icon links grouped with the phone instead of styled as competing CTAs. Keep navigation on real anchors only, maintain `next/image` for assets, and preserve a calm flex-based layout that fits the existing max-width container. Do not add a mobile drawer, do not introduce new sections, and do not move the contact group into the hero.</action>
  <verify>
    <automated>npm.cmd run typecheck</automated>
  </verify>
  <done>The header visibly satisfies SITE-03 with clickable phone and messenger actions plus city context, while preserving the approved contact hierarchy.</done>
</task>

</tasks>

<verification>
Run `npm.cmd run lint` and `npm.cmd run typecheck`. In desktop layout, the contact cluster should show phone + city + messenger icons without appearing more prominent than the hero CTA block.
</verification>

<success_criteria>
The header exposes real contact actions and service-area relevance on the first screen using the shared phase contract.
</success_criteria>

<output>
After completion, create `.planning/phases/01-offer-backbone-and-first-screen-refinement/01-02-SUMMARY.md`
</output>
