---
phase: 01-offer-backbone-and-first-screen-refinement
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/landing/first-screen-content.ts
  - src/features/landing/first-screen.tsx
autonomous: true
requirements:
  - CONT-01
  - CONT-02
  - SITE-03
must_haves:
  truths:
    - "(CONT-01) First-screen copy explicitly states manufacturing, installation, and repair of PVC/aluminum windows, doors, and balconies."
    - "(CONT-02) First-screen primary and secondary actions are real above-the-fold links, not inert buttons."
    - "(SITE-03) Header navigation and contact entrypoints use real in-page/service links with no dead anchors."
  artifacts:
    - path: "src/features/landing/first-screen-content.ts"
      provides: "Typed first-screen content contract with locked hero text and CTA labels"
      contains: "export type FirstScreenContent"
    - path: "src/features/landing/first-screen.tsx"
      provides: "First-screen composition wired only through shared content contract"
      contains: "firstScreenContent"
  key_links:
    - from: "src/features/landing/first-screen.tsx"
      to: "src/features/landing/first-screen-content.ts"
      via: "imported content contract"
      pattern: "from \"@/features/landing/first-screen-content\""
    - from: "src/features/landing/first-screen-content.ts"
      to: "#lead-form|#calculator|#top"
      via: "cta/navigation href values"
      pattern: "#lead-form|#calculator|#top"
---

<objective>
Create the locked content/wiring contract for first-screen correction before styling passes.

Purpose: Freeze decision-driven inputs (copy, CTA labels, targets, contacts) so header and hero styling plans can execute in parallel without reinterpretation.
Output: A typed first-screen contract and a composition root consuming that contract.
</objective>

<execution_context>
@C:/Users/fm/Documents/Business/Saferplast_main/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/fm/Documents/Business/Saferplast_main/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-offer-backbone-and-first-screen-refinement/01-CONTEXT.md
@.planning/phases/01-offer-backbone-and-first-screen-refinement/01-RESEARCH.md
@src/features/landing/first-screen-content.ts
@src/features/landing/first-screen.tsx
@src/features/landing/site-header.tsx
@src/features/landing/hero-section.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock first-screen content contract to decisions D-07/D-08/D-09 and contact/nav scope</name>
  <read_first>
    .planning/phases/01-offer-backbone-and-first-screen-refinement/01-CONTEXT.md
    .planning/REQUIREMENTS.md
    src/features/landing/first-screen-content.ts
  </read_first>
  <files>src/features/landing/first-screen-content.ts</files>
  <acceptance_criteria>
    The file exports `FirstScreenContent` and `firstScreenContent`.
    `headline` equals D-07 locked string exactly.
    `description` equals D-08 locked string exactly.
    CTA labels are exactly D-09 locked values with `#lead-form` primary and `#calculator` secondary hrefs.
    `navigationLinks` include only `#top`, `#calculator`, and `#lead-form`.
    No content references deferred sections/features.
  </acceptance_criteria>
  <behavior>
    - Test 1: Locked headline and subheadline strings are present exactly once in the exported object.
    - Test 2: Primary CTA label is `Бесплатный замер` and secondary CTA label is `Получить расчет` with correct href mapping.
    - Test 3: Navigation hrefs do not include `#projects` or `#contacts`.
  </behavior>
  <action>Update `first-screen-content.ts` so it is the only Phase 01 source for hero/header literals. Set explicit values per decisions: headline D-07 text, description D-08 text, CTA labels D-09 with hrefs `#lead-form` and `#calculator`. Keep `phoneHref`, `phoneLabel`, `cityLabel`, `whatsappHref`, and `telegramHref` in the same contract for header use. Keep trust list in this file but do not add any lower-section copy or new fields for deferred features.</action>
  <verify>
    <automated>npm.cmd run typecheck</automated>
  </verify>
  <done>All locked copy/CTA decisions are centralized and type-safe, with no dead or out-of-scope targets.</done>
</task>

<task type="auto">
  <name>Task 2: Wire FirstScreen composition strictly through shared contract</name>
  <read_first>
    src/features/landing/first-screen.tsx
    src/features/landing/first-screen-content.ts
  </read_first>
  <files>src/features/landing/first-screen.tsx</files>
  <acceptance_criteria>
    `FirstScreen` imports `firstScreenContent` once.
    `SiteHeader` receives nav/contact props from `firstScreenContent`.
    The file does not redefine nav/contact literals inline.
  </acceptance_criteria>
  <action>Keep `src/features/landing/first-screen.tsx` as composition root and pass `navigationLinks`, `phoneHref`, `phoneLabel`, `cityLabel`, `whatsappHref`, and `telegramHref` directly from `firstScreenContent` into `SiteHeader`. Do not alter lower sections, routing structure, or hero image ownership.</action>
  <verify>
    <automated>npm.cmd run lint</automated>
  </verify>
  <done>Wave-2 header and hero work can execute independently against one stable first-screen contract.</done>
</task>

</tasks>

<verification>
Run `npm.cmd run lint` and `npm.cmd run typecheck`. Confirm no dead anchors (`#projects`, `#contacts`) exist in first-screen contract or composition.
</verification>

<success_criteria>
Phase 01 has a locked, typed first-screen contract that fully preserves decision fidelity and enables parallel header/hero implementation.
</success_criteria>

<output>
After completion, create `.planning/phases/01-offer-backbone-and-first-screen-refinement/01-01-SUMMARY.md`
</output>