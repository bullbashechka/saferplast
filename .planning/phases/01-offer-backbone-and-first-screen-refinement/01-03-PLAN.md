---
phase: 01-offer-backbone-and-first-screen-refinement
plan: "03"
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - src/features/landing/hero-section.tsx
autonomous: true
requirements:
  - CONT-01
  - CONT-02
  - CONT-05
  - TRST-01
must_haves:
  truths:
    - "Visitors immediately understand that SaFerplast manufactures, installs, and repairs PVC and aluminum windows, doors, and balconies."
    - "Visitors see a dominant primary CTA for free measurement and a clearly secondary estimate CTA above the fold."
    - "Visitors can see compact trust signals and audience coverage in the hero without scrolling into later sections."
  artifacts:
    - path: "src/features/landing/hero-section.tsx"
      provides: "Hero copy, CTA hierarchy, trust items, audience line, and image layout"
      contains: "primaryCta"
  key_links:
    - from: "src/features/landing/hero-section.tsx"
      to: "src/features/landing/first-screen-content.ts"
      via: "imported hero copy and CTA metadata"
      pattern: "firstScreenContent"
    - from: "src/features/landing/hero-section.tsx"
      to: "#lead-form|#calculator"
      via: "CTA href values"
      pattern: "#lead-form|#calculator"
---

<objective>
Refine the hero so the offer, CTA hierarchy, trust signals, and audience fit are explicit in the first screen.

Purpose: Deliver the actual conversion and messaging outcome of Phase 1 without leaking lower-page scope into the hero.
Output: A responsive `HeroSection` driven by the shared content contract and aligned to the locked first-screen decisions.
</objective>

<execution_context>
@C:/Users/fm/Documents/Business/Saferplast_main/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/fm/Documents/Business/Saferplast_main/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-offer-backbone-and-first-screen-refinement/01-CONTEXT.md
@.planning/phases/01-offer-backbone-and-first-screen-refinement/01-RESEARCH.md
@docs/DESIGN_SYSTEM.md
@src/features/landing/hero-section.tsx
@src/features/landing/first-screen-content.ts
@public/images/herophotogirl.png

<interfaces>
Expected from src/features/landing/first-screen-content.ts:
```ts
export const firstScreenContent: FirstScreenContent;
// Includes:
// - headline
// - description
// - audienceLine
// - trustItems
// - primaryCta
// - secondaryCta
```

Current hero structure:
```tsx
export function HeroSection() {
  return (
    <section aria-labelledby="hero-title">
      <h1 id="hero-title">...</h1>
      <p>...</p>
      <button type="button">Бесплатный замер</button>
      <button type="button">Получить расчет</button>
    </section>
  );
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace hardcoded hero copy and inert buttons with the approved CTA/content model</name>
  <read_first>
    src/features/landing/hero-section.tsx
    src/features/landing/first-screen-content.ts
    .planning/phases/01-offer-backbone-and-first-screen-refinement/01-CONTEXT.md
  </read_first>
  <files>src/features/landing/hero-section.tsx</files>
  <acceptance_criteria>
    `src/features/landing/hero-section.tsx` imports `firstScreenContent`.
    The file renders the D-01 headline and D-02 support text from shared content rather than inline literals.
    The primary action uses `href="#lead-form"` and the secondary action uses `href="#calculator"`.
    The primary CTA is styled as filled brand action and the secondary CTA is outlined or otherwise visually subordinate.
  </acceptance_criteria>
  <action>Refactor the hero to consume `headline`, `description`, `primaryCta`, and `secondaryCta` from `firstScreenContent`. Implement D-03 by keeping the copy explicit about manufacturing, installation, and repair in the visible hero text. Replace the no-op buttons with real links or link-styled controls targeting `#lead-form` and `#calculator`. Preserve the current two-column structure, but keep the measurement CTA visually dominant per D-04 through D-06 and do not introduce form logic or modal behavior.</action>
  <verify>
    <automated>npm.cmd run lint</automated>
  </verify>
  <done>The hero exposes real above-the-fold conversion actions and approved copy instead of placeholder button behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Add compact trust and audience messaging inside the hero without expanding scope</name>
  <read_first>
    src/features/landing/hero-section.tsx
    src/features/landing/first-screen-content.ts
    docs/DESIGN_SYSTEM.md
    .planning/phases/01-offer-backbone-and-first-screen-refinement/01-CONTEXT.md
  </read_first>
  <files>src/features/landing/hero-section.tsx</files>
  <acceptance_criteria>
    The hero file renders all three D-08 trust items from `trustItems`.
    The hero file renders an audience line that mentions apartments, private houses, offices, and commercial spaces.
    The file does not contain the heading `Почему к нам обращаются`.
    Trust items are rendered as compact inline chips, bullets, or short rows within the hero flow rather than a separate card grid section.
  </acceptance_criteria>
  <action>Add the short trust/value treatment required by D-07 and D-08 inside the hero content column, adjacent to the support text and CTA block. Render the audience coverage line for CONT-05 in the same readable flow so visitors can immediately see the business serves apartments, private houses, offices, and commercial spaces. Follow the design-system palette and rem-based spacing rules, keep the image layout intact, and preserve a calm first-screen scan path. Do not create the deferred `Почему к нам обращаются` section, do not add extra cards, and do not introduce absolute-positioned desktop-only layout tricks.</action>
  <verify>
    <automated>npm.cmd run typecheck</automated>
  </verify>
  <done>The hero satisfies the offer, trust, audience, and CTA requirements within a compact first-screen layout that stays inside Phase 1 scope.</done>
</task>

</tasks>

<verification>
Run `npm.cmd run lint` and `npm.cmd run typecheck`. Then visually verify that the hero shows the offer, trust chips, audience line, and CTA hierarchy above the fold without introducing the deferred advantages section.
</verification>

<success_criteria>
The first screen communicates offer, audience, trust, and primary action immediately from the hero itself, with no inert CTAs and no spillover into later landing sections.
</success_criteria>

<output>
After completion, create `.planning/phases/01-offer-backbone-and-first-screen-refinement/01-03-SUMMARY.md`
</output>
