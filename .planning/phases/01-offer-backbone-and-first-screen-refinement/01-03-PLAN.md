---
phase: 01-offer-backbone-and-first-screen-refinement
plan: "03"
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - src/features/landing/hero-section.tsx
autonomous: true
requirements:
  - CONT-01
  - CONT-02
  - CONT-05
  - TRST-01
must_haves:
  truths:
    - "(CONT-01) Hero above the fold clearly states the full offer scope (manufacture/install/repair, PVC+aluminum, windows/doors/balconies)."
    - "(CONT-02) Hero has exactly two CTAs with locked labels and primary/secondary hierarchy."
    - "(CONT-05) Hero includes explicit audience coverage line for apartments, private houses, offices and commercial spaces."
    - "(TRST-01) Hero shows differentiator chips including own production, direct pricing, and speed of work."
  artifacts:
    - path: "src/features/landing/hero-section.tsx"
      provides: "Hero layout and style mapping from demo values with locked text/cta behavior"
      contains: "herophotogirl.png"
  key_links:
    - from: "src/features/landing/hero-section.tsx"
      to: "docs/heroDemoStyles.md"
      via: "tailwind mapping of headline/body/cta dimensions"
      pattern: "3.4375rem|4.25rem|0.9375rem|3.0625rem"
    - from: "src/features/landing/hero-section.tsx"
      to: "src/features/landing/first-screen-content.ts"
      via: "locked copy and CTA labels from shared contract"
      pattern: "headline|description|primaryCta|secondaryCta|trustItems|audienceLine"
---

<objective>
Реализовать Figma-driven hero correction через mapping `docs/heroDemoStyles.md` values в valid Tailwind при сохранении locked copy/actions.

Purpose: Дать offer/CTA/trust/audience outcome в первом экране только, без расширения нижних секций.
Output: Обновленный `hero-section.tsx`, выровненный по demo style values и locked decision IDs.
</objective>

<execution_context>
@C:/Users/fm/Documents/Business/Saferplast_main/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/fm/Documents/Business/Saferplast_main/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-offer-backbone-and-first-screen-refinement/01-CONTEXT.md
@docs/heroDemoStyles.md
@src/features/landing/hero-section.tsx
@src/features/landing/first-screen-content.ts
@public/images/herophotogirl.png
</context>

<tasks>

<task type="auto">
  <name>Task 1: Map locked headline/subheadline/CTA geometry to Tailwind values</name>
  <read_first>
    docs/heroDemoStyles.md
    .planning/phases/01-offer-backbone-and-first-screen-refinement/01-CONTEXT.md
    src/features/landing/hero-section.tsx
    src/features/landing/first-screen-content.ts
  </read_first>
  <files>src/features/landing/hero-section.tsx</files>
  <acceptance_criteria>
    Hero reads headline/subheadline and CTA labels from `firstScreenContent` (no inline copy literals).
    Headline maps to 55px/100% (`text-[3.4375rem] leading-[1]`) using display font and `#004B62` color.
    Description maps to 16px text (`text-[1rem]`) and remains directly below headline.
    Primary CTA maps to 68px min-height (`min-h-[4.25rem]`), 15px radius (`rounded-[0.9375rem]`), 49px horizontal padding (`px-[3.0625rem]`), and filled `#004B62` background.
    Secondary CTA matches size and radius with 1px `#004B62` border and transparent fill.
    Hero contains exactly two CTAs with locked labels: `���������� �����` and `�������� ������`.
  </acceptance_criteria>
  <action>В `hero-section.tsx` map `docs/heroDemoStyles.md` values directly to Tailwind classes: headline 55px -> `text-[3.4375rem]`, body 16px -> `text-[1rem]`, both with `leading-[1]` or specified line height. Apply CTA geometry from demo: height 68px (`min-h-[4.25rem]`), radius 15px (`rounded-[0.9375rem]`), horizontal padding 49px (`px-[3.0625rem]`), text 20px (`text-[1.25rem] font-medium`). Keep primary filled `bg-[#004B62] text-white` and secondary outlined `border border-[#004B62] text-[#004B62]`. Use only content from `firstScreenContent` for D-07/D-08/D-09 locks. Do not add a third CTA, modal, or form logic.</action>
  <verify>
    <automated>npm.cmd run lint</automated>
  </verify>
  <done>Hero typography and CTA block are concrete Tailwind mappings of demo values with locked labels and hierarchy preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Preserve image scope and add trust/audience cues inside hero flow only</name>
  <read_first>
    .planning/phases/01-offer-backbone-and-first-screen-refinement/01-CONTEXT.md
    docs/heroDemoStyles.md
    src/features/landing/hero-section.tsx
    src/features/landing/first-screen-content.ts
  </read_first>
  <files>src/features/landing/hero-section.tsx</files>
  <acceptance_criteria>
    Hero image source remains `/images/herophotogirl.png` and image container stays on right side for desktop.
    Audience line explicitly names apartments, private houses, offices, and commercial spaces.
    Trust chips are rendered in hero flow and include direct pricing differentiator (`���� ��� �����������`).
    Hero file does not introduce deferred lower section blocks, FAQ, reviews, or service cards.
  </acceptance_criteria>
  <action>Keep the existing right photo unchanged per D-11: keep `src="/images/herophotogirl.png"` and existing two-column layout where text/CTA stays left and photo right on desktop. Render `audienceLine` and `trustItems` in compact hero-level chips/rows below description and near CTA block to satisfy CONT-05 and TRST-01 without new sections. Include differentiators from context (own production, direct pricing, speed of work, 1-year guarantee) via `trustItems`. Do not modify photo asset/cropping strategy, do not create a separate advantages section, and do not touch any section below the first screen.</action>
  <verify>
    <automated>npm.cmd run typecheck</automated>
  </verify>
  <done>Hero satisfies offer, audience, and trust requirements while keeping right-side photo and strict first-screen-only scope.</done>
</task>

</tasks>

<verification>
Run `npm.cmd run lint` and `npm.cmd run typecheck`. Verify `hero-section.tsx` still uses `/images/herophotogirl.png` and contains exactly two locked CTA labels.
</verification>

<success_criteria>
Hero is corrected to demo style intent with locked copy/actions and no scope creep beyond Header + Hero.
</success_criteria>

<output>
After completion, create `.planning/phases/01-offer-backbone-and-first-screen-refinement/01-03-SUMMARY.md`
</output>