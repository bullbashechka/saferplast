# Deferred Items

## 2026-03-30

- Repository-wide `npm.cmd run lint` fails in `.codex/get-shit-done/**/*.cjs` with `@typescript-eslint/no-require-imports` errors (pre-existing, unrelated to plan `03-01` scope). Task-level changes pass `npm.cmd run typecheck`.
