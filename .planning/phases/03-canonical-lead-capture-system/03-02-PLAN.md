---
phase: 03-canonical-lead-capture-system
plan: "02"
type: execute
wave: 2
depends_on:
  - "03-01"
files_modified:
  - src/features/calculator/calculator-section.tsx
  - src/features/calculator/calculator-cards.tsx
  - src/features/calculator/calculator-entry-modal.tsx
  - src/features/calculator/calculator-content.ts
autonomous: true
requirements:
  - CALC-01
  - CALC-02
  - CALC-03
  - CALC-04
must_haves:
  truths:
    - "User can choose one of four calculator categories and start estimate flow."
    - "UI clearly states result is an approximate estimate."
    - "Interaction remains lightweight and does not become a deep configurator."
    - "Desktop hover and mobile tap both provide an actionable path."
  artifacts:
    - path: "src/features/calculator/calculator-cards.tsx"
      provides: "Interactive card grid with hover/tap behavior"
    - path: "src/features/calculator/calculator-entry-modal.tsx"
      provides: "Modal entrypoint opened by calculator CTA"
    - path: "src/features/calculator/calculator-section.tsx"
      provides: "Integrated section composition and disclaimer"
  key_links:
    - from: "src/features/calculator/calculator-cards.tsx"
      to: "src/features/calculator/calculator-entry-modal.tsx"
      via: "open modal callback from CTA action"
      pattern: "onOpenModal|setIsModalOpen"
    - from: "src/features/calculator/calculator-section.tsx"
      to: "src/features/calculator/calculator-content.ts"
      via: "content and disclaimer rendering"
      pattern: "calculatorSectionContent"
    - from: "src/features/calculator/calculator-cards.tsx"
      to: "src/features/calculator/calculator-types.ts"
      via: "typed category/context usage"
      pattern: "CalculatorCategoryKey|CalculatorContextDraft"
---

<objective>
Implement the interactive calculator section experience defined in Phase 03 decisions: responsive card grid, hover/tap CTA behavior, modal entrypoint, and explicit estimate disclaimer.

Purpose: Deliver a conversion-oriented lightweight estimate interaction while keeping deferred heavy logic out of scope.
Output: Working card interactions and modal trigger that satisfy CALC-01..03 and respect CALC-04 contract boundaries.
</objective>

<execution_context>
@C:/Users/fm/Documents/Business/Saferplast_main/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/fm/Documents/Business/Saferplast_main/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-canonical-lead-capture-system/03-CONTEXT.md
@.planning/phases/03-canonical-lead-capture-system/03-RESEARCH.md
@.planning/phases/03-canonical-lead-capture-system/03-01-SUMMARY.md
@src/features/calculator/calculator-section.tsx
@src/features/calculator/calculator-content.ts
@src/features/calculator/calculator-types.ts

<interfaces>
From src/features/calculator/calculator-types.ts (from 03-01):
```ts
export type CalculatorCategoryKey = "window" | "door" | "balcony" | "windowsill";
export type CalculatorContextDraft = {
  category: CalculatorCategoryKey;
  source: "calculator-section";
  createdAtIso: string;
};
```

From src/features/calculator/calculator-content.ts (from 03-01):
```ts
export const calculatorSectionContent = {
  title: "Рассчитайте примерную стоимость",
  subtitle: "Выберите, что именно нужно рассчитать, и получите предварительный расчет за несколько шагов",
  cards: [/* 4 items */],
  cta: { label: "Рассчитать", iconSrc: "/images/svg-on-button-рассчитать.svg" }
};
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build interactive cards component with desktop hover and mobile tap parity</name>
  <files>src/features/calculator/calculator-cards.tsx, src/features/calculator/calculator-content.ts</files>
  <read_first>.planning/phases/03-canonical-lead-capture-system/03-CONTEXT.md, src/features/calculator/calculator-content.ts, src/features/landing/advantages-section.tsx</read_first>
  <action>Create `calculator-cards.tsx` as a client component that renders exactly four cards per D-05 using mapped image assets (`/images/window.jpg`, `/images/door.png`, `/images/balcony.png`, `/images/windowsill.png`). Implement desktop geometry/interactions per D-06..D-11 with explicit horizontal desktop arrangement per D-07 (single-row cards at desktop widths), card 285x210, hover to 152 height with reveal button 286x50, and CTA button background color fixed to `hsla(194, 100%, 19%, 1)` per D-12. Implement mobile 2-column geometry per D-17..D-18 (145x106, tap to reveal action). Apply gradient overlay and arrow placement per D-08..D-09. Keep interaction logic minimal and category-based only (no deep configurator or formula, per CALC-03 and deferred items).</action>
  <verify>
    <automated>npm.cmd run lint && npm.cmd run typecheck</automated>
  </verify>
  <acceptance_criteria>Desktop cards are arranged horizontally in one row at desktop breakpoints (D-07), show hover collapse + CTA reveal and restore on pointer leave (D-10, D-16), and CTA button color is exactly `hsla(194, 100%, 19%, 1)` (D-12); mobile viewport shows 2-column cards with tappable action path; cards remain exactly four categories with no additional inputs/steps.</acceptance_criteria>
  <done>Interactive card grid is complete and ready to open modal from each CTA action.</done>
</task>

<task type="auto">
  <name>Task 2: Implement lightweight calculator modal entrypoint and wire section composition</name>
  <files>src/features/calculator/calculator-entry-modal.tsx, src/features/calculator/calculator-section.tsx, src/features/calculator/calculator-cards.tsx</files>
  <read_first>src/features/calculator/calculator-section.tsx, src/features/calculator/calculator-cards.tsx, src/features/calculator/calculator-types.ts, .planning/phases/03-canonical-lead-capture-system/03-RESEARCH.md</read_first>
  <action>Create `calculator-entry-modal.tsx` (client) as lightweight modal entrypoint opened by the card CTA action per D-15. Modal should capture selected category into `CalculatorContextDraft` and display concise estimate-entry placeholder with explicit disclaimer text that result is approximate estimate (CALC-02). Wire modal open/close state through `calculator-section.tsx` and pass open callbacks into `calculator-cards.tsx`. Keep scope intentionally minimal: no full multi-step modal workflow, no price formula engine, no lead-form transfer implementation (deferred).</action>
  <verify>
    <automated>npm.cmd run lint && npm.cmd run typecheck</automated>
  </verify>
  <acceptance_criteria>Click/tap on any card CTA opens modal with selected category context; disclaimer is visible in section and/or modal in plain language that value is estimate; close control reliably returns UI to prior state.</acceptance_criteria>
  <done>Calculator interaction supports entrypoint-to-modal flow while preserving phase scope boundaries.</done>
</task>

</tasks>

<verification>
Run `npm.cmd run lint && npm.cmd run typecheck`. Manually verify desktop hover interaction and mobile tap behavior in browser responsive mode, and confirm modal opens from all four categories.
</verification>

<success_criteria>
Users can start an approximate estimate flow from category cards with clear estimate framing, lightweight inputs only, and preserved typed context for future lead-flow handoff.
</success_criteria>

<output>
After completion, create `.planning/phases/03-canonical-lead-capture-system/03-02-SUMMARY.md`
</output>
