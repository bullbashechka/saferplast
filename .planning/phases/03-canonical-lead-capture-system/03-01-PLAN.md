---
phase: 03-canonical-lead-capture-system
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/page.tsx
  - src/features/calculator/calculator-content.ts
  - src/features/calculator/calculator-types.ts
  - src/features/calculator/calculator-section.tsx
  - public/images/svg-on-button-рассчитать.svg
autonomous: true
requirements:
  - FLOW-01
  - CALC-04
must_haves:
  truths:
    - "Calculator section is rendered immediately after advantages in the landing flow."
    - "Calculator section has a typed context contract ready for later lead-form handoff."
    - "Section-level heading and subtitle match locked copy and typography constraints."
  artifacts:
    - path: "src/app/page.tsx"
      provides: "Canonical section order with calculator after advantages"
    - path: "src/features/calculator/calculator-content.ts"
      provides: "Locked content contract for calculator section and cards"
    - path: "src/features/calculator/calculator-types.ts"
      provides: "Typed calculator context contract for future lead-flow integration"
    - path: "public/images/svg-on-button-рассчитать.svg"
      provides: "Canonical CTA icon path per locked decision"
  key_links:
    - from: "src/app/page.tsx"
      to: "src/features/calculator/calculator-section.tsx"
      via: "HomePage composition order"
      pattern: "AdvantagesSection.*CalculatorSection"
    - from: "src/features/calculator/calculator-section.tsx"
      to: "src/features/calculator/calculator-content.ts"
      via: "typed content import"
      pattern: "from \"@/features/calculator/calculator-content\""
    - from: "src/features/calculator/calculator-content.ts"
      to: "public/images/svg-on-button-рассчитать.svg"
      via: "icon path constant"
      pattern: "svg-on-button-рассчитать\\.svg"
---

<objective>
Prepare the canonical calculator foundation for Phase 03 by fixing landing flow placement, locking approved content decisions, and defining a reusable typed context contract for future lead-flow handoff.

Purpose: Remove integration risk before interactive implementation and guarantee requirement traceability to FLOW-01 and CALC-04.
Output: Stable section order, typed contracts, and canonical assets/section shell ready for interaction work.
</objective>

<execution_context>
@C:/Users/fm/Documents/Business/Saferplast_main/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/fm/Documents/Business/Saferplast_main/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-canonical-lead-capture-system/03-CONTEXT.md
@.planning/phases/03-canonical-lead-capture-system/03-RESEARCH.md
@src/app/page.tsx
@src/features/calculator/calculator-section.tsx
@src/features/landing/advantages-section.tsx

<interfaces>
From src/app/page.tsx:
```tsx
export default function HomePage() {
  return (
    <main>
      <FirstScreen />
      <AdvantagesSection />
      <ProofSection />
      <CalculatorSection />
      <LeadFormSection />
    </main>
  );
}
```

From src/features/calculator/calculator-section.tsx:
```tsx
export function CalculatorSection() {
  return <section id="calculator">...</section>;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create typed calculator contracts and lock approved content</name>
  <files>src/features/calculator/calculator-types.ts, src/features/calculator/calculator-content.ts, public/images/svg-on-button-рассчитать.svg</files>
  <read_first>.planning/phases/03-canonical-lead-capture-system/03-CONTEXT.md, .planning/phases/03-canonical-lead-capture-system/03-RESEARCH.md, src/features/landing/advantages-section-content.ts</read_first>
  <action>Create `calculator-types.ts` with typed contracts for category keys and `CalculatorContextDraft` (for future lead-flow handoff, per CALC-04 and deferred-scope boundary). Create `calculator-content.ts` as single source of truth for section heading/subheading and four cards (`окно`, `дверь`, `балкон`, `подоконник`) per D-01, D-03, D-05, and include typography metadata fields for D-02/D-04. Add CTA metadata with fixed label `Рассчитать` and icon path `/images/svg-on-button-рассчитать.svg` per D-13 and D-14. Copy existing icon asset from `public/icons/svg-on-button-рассчитать.svg` into `public/images/svg-on-button-рассчитать.svg` so implementation follows locked path instead of introducing a fallback.</action>
  <verify>
    <automated>npm.cmd run lint && npm.cmd run typecheck</automated>
  </verify>
  <acceptance_criteria>All contract files export strict TypeScript types with no `any`; card tuple length is exactly 4; icon path resolves to `/images/svg-on-button-рассчитать.svg`; no formula or deep-step calculator logic is introduced.</acceptance_criteria>
  <done>Contracts are committed and can be consumed without additional discovery.</done>
</task>

<task type="auto">
  <name>Task 2: Reorder page flow and replace placeholder shell with contract-driven section scaffold</name>
  <files>src/app/page.tsx, src/features/calculator/calculator-section.tsx</files>
  <read_first>src/app/page.tsx, src/features/calculator/calculator-section.tsx, src/features/landing/advantages-section.tsx, src/features/calculator/calculator-content.ts</read_first>
  <action>In `src/app/page.tsx`, move `CalculatorSection` immediately after `AdvantagesSection` and before `ProofSection` to satisfy FLOW-01. Refactor `calculator-section.tsx` to consume `calculator-content.ts` and render semantic heading/subheading with centered layout, preserving Tailwind-first approach and no absolute-position layout. Keep section scoped to shell+content wiring only; do not implement full modal scenario or pricing formula (deferred).</action>
  <verify>
    <automated>npm.cmd run lint && npm.cmd run typecheck</automated>
  </verify>
  <acceptance_criteria>Render order is `FirstScreen -> AdvantagesSection -> CalculatorSection -> ProofSection -> LeadFormSection`; heading/subheading text exactly match D-01 and D-03; section keeps id `calculator`; no deferred features are implemented.</acceptance_criteria>
  <done>Landing flow and calculator shell are stable and ready for interactive card implementation.</done>
</task>

</tasks>

<verification>
Run `npm.cmd run lint && npm.cmd run typecheck`. Confirm source order in `src/app/page.tsx` places calculator directly after advantages.
</verification>

<success_criteria>
FLOW-01 is structurally enforced in source code, and CALC-04 has a typed context artifact ready for downstream lead-form phases without implementing deferred transfer mechanics.
</success_criteria>

<output>
After completion, create `.planning/phases/03-canonical-lead-capture-system/03-01-SUMMARY.md`
</output>

