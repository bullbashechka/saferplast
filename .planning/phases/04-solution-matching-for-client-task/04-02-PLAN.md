---
phase: 04-solution-matching-for-client-task
plan: "02"
type: execute
wave: 2
depends_on:
  - "04-01"
files_modified:
  - src/features/landing/solution-matching-section.tsx
  - src/app/page.tsx
autonomous: true
requirements:
  - FLOW-02
  - CONT-03
  - CONT-04
must_haves:
  truths:
    - "User sees section `Подберем решение под вашу задачу` directly after calculator."
    - "User can read five scenario cards as one coherent 3+2 semantic block."
    - "User can open `#lead-form` from every card via `Узнать подробнее`."
  artifacts:
    - path: "src/features/landing/solution-matching-section.tsx"
      provides: "Semantic responsive section rendering from locked content contract"
    - path: "src/app/page.tsx"
      provides: "FLOW-02-compliant section order in home page composition"
  key_links:
    - from: "src/features/landing/solution-matching-section.tsx"
      to: "src/features/landing/solution-matching-content.ts"
      via: "content-driven card rendering"
      pattern: "solutionMatchingContent"
    - from: "src/features/landing/solution-matching-section.tsx"
      to: "#lead-form"
      via: "card CTA anchors"
      pattern: "href=\"#lead-form\"|ctaHref"
    - from: "src/app/page.tsx"
      to: "src/features/calculator/calculator-section.tsx"
      via: "direct adjacency before solution matching section"
      pattern: "CalculatorSection"
---

<objective>
Render and wire the Phase 04 section as a production-ready, responsive, semantic block directly after the calculator while preserving the canonical lead-form route.

Purpose: Fulfill FLOW-02 and contextual content requirements with locked visuals/copy and zero ambiguity in section placement.
Output: New landing feature component and updated page composition order.
</objective>

<execution_context>
@C:/Users/fm/Documents/Business/Saferplast_main/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/fm/Documents/Business/Saferplast_main/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-solution-matching-for-client-task/04-CONTEXT.md
@.planning/phases/04-solution-matching-for-client-task/04-RESEARCH.md
@.planning/phases/04-solution-matching-for-client-task/04-VALIDATION.md
@.planning/phases/04-solution-matching-for-client-task/04-01-PLAN.md
@src/app/page.tsx
@src/features/calculator/calculator-section.tsx
@src/features/landing/proof-section.tsx
@src/features/landing/solution-matching-content.ts
@src/features/landing/solution-matching-types.ts

<interfaces>
From src/app/page.tsx:
```tsx
<FirstScreen />
<AdvantagesSection />
<ProofSection />
<CalculatorSection />
<LeadFormSection />
```

From src/features/landing/solution-matching-content.ts (from 04-01):
```ts
export const solutionMatchingContent = {
  heading: "Подберем решение под вашу задачу",
  subtitle: "...",
  topCards: [/* 3 cards */],
  bottomCards: [/* 2 cards */]
};
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build semantic responsive section component from locked contract</name>
  <files>src/features/landing/solution-matching-section.tsx</files>
  <read_first>.planning/phases/04-solution-matching-for-client-task/04-CONTEXT.md, src/features/landing/proof-section.tsx, src/features/landing/solution-matching-content.ts</read_first>
  <action>Create `solution-matching-section.tsx` that renders a semantic `<section>` with `<h2>` and subtitle paragraph using exact text from D-01 and D-03, and renders cards via mapped contract data (no inline copy). Implement heading typography per D-02 with exact class/token values: `font-['Sansation'] font-normal text-[44px] leading-[1] tracking-[0]`. Implement subtitle typography per D-04 with exact class/token values: `font-body font-normal text-[16px] leading-[1] tracking-[0] text-center`. Implement desktop 3+2 card composition per D-05 with separate top and bottom grids, and responsive mobile stacking while preserving card order. Apply locked visual targets using Tailwind-first classes: rounded corners `20px` (D-06..D-08), inner text container radius/padding `10px` (D-10, D-16), heading color `hsla(194, 100%, 19%, 1)` (D-20), subtitle color `hsla(0, 0%, 14%, 1)` (D-21), top-card white gradient overlay (D-09), and bottom-left composite overlay (D-15). Render each card as `<article>` with `next/image` and anchor CTA `Узнать подробнее` with underline style per D-23 linked to `#lead-form` per D-24.</action>
  <verify>
    <automated>npm.cmd exec eslint src/features/landing/solution-matching-section.tsx && npm.cmd run typecheck</automated>
  </verify>
  <acceptance_criteria>Component renders exactly 5 cards from contract data, all as semantic articles; top row has 3 entries and bottom row has 2 entries on desktop; heading uses exact D-02 typography classes/tokens `font-['Sansation'] font-normal text-[44px] leading-[1] tracking-[0]`; subtitle uses exact D-04 typography classes/tokens `font-body font-normal text-[16px] leading-[1] tracking-[0] text-center`; each card contains visible `Узнать подробнее` anchor to `#lead-form`; no card copy is hardcoded in JSX.</acceptance_criteria>
  <done>Section component is ready for page-level integration and satisfies locked UI/content behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Wire section order in homepage directly after calculator (FLOW-02)</name>
  <files>src/app/page.tsx</files>
  <read_first>src/app/page.tsx, src/features/calculator/calculator-section.tsx, src/features/landing/solution-matching-section.tsx, .planning/REQUIREMENTS.md</read_first>
  <action>Import `SolutionMatchingSection` in `src/app/page.tsx` and place it immediately after `CalculatorSection` to satisfy FLOW-02. Keep `LeadFormSection` route compatibility intact by preserving `#lead-form` behavior and not changing form id/anchors. Do not introduce unrelated section re-architecture; only perform the minimal composition change required by FLOW-02.</action>
  <verify>
    <automated>npm.cmd exec eslint src/app/page.tsx src/features/landing/solution-matching-section.tsx && npm.cmd run typecheck</automated>
  </verify>
  <acceptance_criteria>Home page composition contains `CalculatorSection` followed immediately by `SolutionMatchingSection`; all card CTAs continue to target `#lead-form`; no unrelated section logic is modified.</acceptance_criteria>
  <done>FLOW-02 is implemented in source order and the new section is fully reachable in the landing flow.</done>
</task>

</tasks>

<verification>
Automated:
- `npm.cmd exec eslint src/app/page.tsx src/features/landing/solution-matching-section.tsx src/features/landing/solution-matching-content.ts src/features/landing/solution-matching-types.ts`
- `npm.cmd run typecheck`

Manual:
- Open landing page and verify section appears immediately after calculator.
- Confirm all five `Узнать подробнее` links navigate to `#lead-form`.
</verification>

<success_criteria>
FLOW-02, CONT-03, and CONT-04 are met by an implemented semantic section with locked copy, five scenario cards, and reliable anchor routing to the lead form.
</success_criteria>

<output>
After completion, create `.planning/phases/04-solution-matching-for-client-task/04-02-SUMMARY.md`
</output>
