---
phase: 04-solution-matching-for-client-task
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/landing/solution-matching-types.ts
  - src/features/landing/solution-matching-content.ts
autonomous: true
requirements:
  - CONT-03
  - CONT-04
must_haves:
  truths:
    - "User sees the exact approved section heading and subtitle for the solution-matching block."
    - "User sees five scenario cards presented as a 3-card top row and a 2-card bottom row."
    - "User can click `Узнать подробнее` on any card and land on the lead form (`#lead-form`)."
  artifacts:
    - path: "src/features/landing/solution-matching-types.ts"
      provides: "Typed contracts for section, rows, cards, sizing and CTA routing"
    - path: "src/features/landing/solution-matching-content.ts"
      provides: "Locked heading/subheading and five-card scenario content"
  key_links:
    - from: "src/features/landing/solution-matching-content.ts"
      to: "src/features/landing/solution-matching-types.ts"
      via: "type-checked content object"
      pattern: "satisfies SolutionMatchingContent|SolutionMatchingContent"
    - from: "src/features/landing/solution-matching-content.ts"
      to: "#lead-form"
      via: "per-card CTA target"
      pattern: "#lead-form"
---

<objective>
Lock the full Phase 04 content and contract surface before rendering to avoid interpretation drift and guarantee that all locked discussion decisions are preserved in implementation.

Purpose: Freeze canonical section data per user-approved decisions and remove ambiguity for the rendering plan.
Output: Typed content/contract files with exact heading, subtitle, five cards, and CTA route.
</objective>

<execution_context>
@C:/Users/fm/Documents/Business/Saferplast_main/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/fm/Documents/Business/Saferplast_main/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-solution-matching-for-client-task/04-CONTEXT.md
@.planning/phases/04-solution-matching-for-client-task/04-RESEARCH.md
@.planning/phases/04-solution-matching-for-client-task/04-VALIDATION.md
@src/features/calculator/calculator-section.tsx

<interfaces>
From src/features/calculator/calculator-section.tsx:
```tsx
export function CalculatorSection() {
  return <section id="calculator">...</section>;
}
```

New contracts this plan must create:
```ts
export type SolutionMatchingCard = {
  id: "cold-noise" | "balcony-turnkey" | "broken-window-door" | "office-commercial" | "new-object";
  title: string;
  subtitle: string;
  imageSrc: string;
  ctaLabel: "Узнать подробнее";
  ctaHref: "#lead-form";
};
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define strict section and card contracts for locked 3+2 structure</name>
  <files>src/features/landing/solution-matching-types.ts</files>
  <read_first>.planning/phases/04-solution-matching-for-client-task/04-CONTEXT.md, .planning/REQUIREMENTS.md, src/features/calculator/calculator-section.tsx</read_first>
  <action>Create `SolutionMatchingContent` contracts that hard-lock row composition per D-05: `topCards` tuple length 3 and `bottomCards` tuple length 2. Encode required geometry/token fields with concrete values per D-06, D-07, D-08, D-10, D-16 (`387x368`, `590x290`, `20px`, `40px 30px`, `10px`), and include constrained CTA route type `"#lead-form"` per D-24. Keep it Tailwind-first and semantic-focused; do not introduce runtime logic or API concerns.</action>
  <verify>
    <automated>npm.cmd exec eslint src/features/landing/solution-matching-types.ts && npm.cmd run typecheck</automated>
  </verify>
  <acceptance_criteria>TypeScript fails if top row is not exactly 3 cards or bottom row is not exactly 2 cards; CTA route type accepts only `#lead-form`; geometry tokens are represented as explicit locked values from D-06..D-08.</acceptance_criteria>
  <done>Contract file enforces structure and prevents accidental scope drift for card count and CTA routing.</done>
</task>

<task type="auto">
  <name>Task 2: Create canonical content source with exact heading, subtitle, and five scenarios</name>
  <files>src/features/landing/solution-matching-content.ts</files>
  <read_first>.planning/phases/04-solution-matching-for-client-task/04-CONTEXT.md, src/features/landing/advantages-section-content.ts, src/features/landing/solution-matching-types.ts</read_first>
  <action>Create `solution-matching-content.ts` as the only source of section copy and card payload, implementing D-01 and D-03 exactly: heading `Подберем решение под вашу задачу`, subtitle `Не нужно во всем разбираться самим. Мы подскажем, что подойдет именно в вашей ситуации`. Add explicit typography token fields for rendering per D-02 and D-04: heading font `Sansation`, weight `400`, size `44px`, line-height `100%`, letter-spacing `0%`; subtitle font `Montserrat`, weight `400`, size `16px`, line-height `100%`, letter-spacing `0%`, text-align `center`. Represent these as exact reusable class/token values in content (for example: `headingClassName: "font-['Sansation'] font-normal text-[44px] leading-[1] tracking-[0]"`, `subtitleClassName: "font-body font-normal text-[16px] leading-[1] tracking-[0] text-center"`). Add five cards with exact titles/subtitles per D-11, D-12, D-13, D-17, D-18 and fixed CTA label `Узнать подробнее` per D-22/D-23, all with `ctaHref: "#lead-form"` per D-24. Include image references under `/images/*` per D-14 and D-19 and lock color tokens per D-20 and D-21.</action>
  <verify>
    <automated>npm.cmd exec eslint src/features/landing/solution-matching-content.ts && npm.cmd run typecheck</automated>
  </verify>
  <acceptance_criteria>All five card records exist with exact locked Russian copy; every card has `ctaLabel` = `Узнать подробнее` and `ctaHref` = `#lead-form`; content includes explicit D-02/D-04 typography tokens with exact values (`font-['Sansation']`, `font-normal`, `text-[44px]`, `leading-[1]`, `tracking-[0]` for heading; `font-body`, `font-normal`, `text-[16px]`, `leading-[1]`, `tracking-[0]`, `text-center` for subtitle); exported object satisfies the strict contracts from `solution-matching-types.ts` without `any` casts.</acceptance_criteria>
  <done>Phase 04 content is fully locked and implementation-ready with no unresolved content decisions.</done>
</task>

</tasks>

<verification>
Confirm contract enforcement and content validity with:
- `npm.cmd exec eslint src/features/landing/solution-matching-types.ts src/features/landing/solution-matching-content.ts`
- `npm.cmd run typecheck`
</verification>

<success_criteria>
Locked decisions D-01..D-24 that affect copy, card set, and CTA target are represented in typed source files, providing deterministic input for rendering and FLOW-02 wiring.
</success_criteria>

<output>
After completion, create `.planning/phases/04-solution-matching-for-client-task/04-01-SUMMARY.md`
</output>
