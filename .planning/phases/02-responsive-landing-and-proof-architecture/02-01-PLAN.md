---
phase: 02-responsive-landing-and-proof-architecture
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/landing/advantages-section-content.ts
  - src/features/landing/advantages-section.tsx
  - src/app/page.tsx
autonomous: true
requirements:
  - CONT-03
  - CONT-04
  - TRST-02
  - TRST-03
  - TRST-04
  - TRST-05
  - SITE-01
  - SITE-02
must_haves:
  truths:
    - "Advantages block is implemented as a standalone semantic landing section with indexable text content."
    - "Section adapts to desktop/tablet/mobile without horizontal overflow."
    - "Tailwind mapping follows docs/preimushestva.md visual contract (light cards + one dark CTA card)."
  artifacts:
    - path: "src/features/landing/advantages-section-content.ts"
      provides: "Typed Russian content contract for headline/subheadline/cards"
      contains: "export const advantagesSectionContent"
    - path: "src/features/landing/advantages-section.tsx"
      provides: "Semantic and responsive advantages section component"
      contains: "<section"
    - path: "src/app/page.tsx"
      provides: "Landing composition includes advantages section"
      contains: "AdvantagesSection"
---

<objective>
Implement Phase 2 narrowed scope: only the advantages block from `docs/preimushestva.md`, including SEO-friendly semantic markup and mobile-first responsiveness.
</objective>

<execution_context>
@C:/Users/fm/Documents/Business/Saferplast_main/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/fm/Documents/Business/Saferplast_main/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-responsive-landing-and-proof-architecture/02-CONTEXT.md
@.planning/phases/02-responsive-landing-and-proof-architecture/02-RESEARCH.md
@docs/preimushestva.md
@src/app/page.tsx
@src/features/landing
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create typed content contract for the advantages block</name>
  <read_first>
    docs/preimushestva.md
    .planning/phases/02-responsive-landing-and-proof-architecture/02-CONTEXT.md
    src/features/landing/first-screen-content.ts
  </read_first>
  <files>src/features/landing/advantages-section-content.ts</files>
  <acceptance_criteria>
    File exports `advantagesSectionContent` with UTF-8 Russian text (no mojibake sequences like `Р`-prefixed broken glyphs).
    `title` is exactly `Почему к нам обращаются`.
    `subtitle` is exactly `Понятные условия, собственное производство и готовое решение`.
    Content includes 6 cards per `docs/preimushestva.md`, including one dark CTA card with label `Бесплатный замер`.
  </acceptance_criteria>
  <action>Create `src/features/landing/advantages-section-content.ts` with strict typed structure: section `title`, `subtitle`, and six card objects (`title`, `description`, `variant`, optional `href`). Fill card copy from `docs/preimushestva.md`, keep CTA card as `variant: "dark"` and `href: "#lead-form"`, and keep all strings UTF-8-clean Russian text.</action>
  <verify>
    <automated>npm.cmd run typecheck</automated>
  </verify>
  <done>Content source is centralized, typed, and safe from encoding regressions.</done>
</task>

<task type="auto">
  <name>Task 2: Build semantic and responsive Tailwind section component</name>
  <read_first>
    docs/preimushestva.md
    src/features/landing/advantages-section-content.ts
    src/features/landing/hero-section.tsx
  </read_first>
  <files>src/features/landing/advantages-section.tsx</files>
  <acceptance_criteria>
    Root element is `<section>` with internal `<h2>` for section title.
    Each card title is rendered as `<h3>` and description as plain text paragraph.
    Desktop layout uses multi-card grid with preserved visual hierarchy.
    Mobile layout becomes one-column card stack with no horizontal overflow.
    Tailwind classes reflect approved style direction: title 44px Sansation on desktop, body 16px Montserrat, card radius 20px, light gradient cards + one dark card.
  </acceptance_criteria>
  <action>Create `src/features/landing/advantages-section.tsx` that imports `advantagesSectionContent` and renders semantic SEO-friendly markup (`section > h2 + p + card list`). Implement responsive layout using Tailwind (`grid`/`flex` at breakpoints) without absolute coordinates. Map visual tokens from `docs/preimushestva.md`: headline `text-[#004B62]`, light card gradient using arbitrary Tailwind gradient values, `rounded-[1.25rem]`, card paddings approximating `40px 30px`, and dark CTA card `bg-[#004B62] text-white` with clear interactive affordance.</action>
  <verify>
    <automated>npm.cmd run lint</automated>
  </verify>
  <done>Advantages block is production-usable, semantic, and responsive while matching approved design language.</done>
</task>

<task type="auto">
  <name>Task 3: Integrate the section into landing composition and verify behavior</name>
  <read_first>
    src/app/page.tsx
    src/features/landing/advantages-section.tsx
  </read_first>
  <files>src/app/page.tsx</files>
  <acceptance_criteria>
    `page.tsx` imports `AdvantagesSection` and renders it in landing flow after first-screen area.
    Existing Phase 1 header/hero composition remains intact.
    `npm.cmd run lint` and `npm.cmd run typecheck` pass.
  </acceptance_criteria>
  <action>Update `src/app/page.tsx` to include `AdvantagesSection` in the page composition in a position consistent with landing narrative flow, without changing existing header/hero behavior. Run lint and typecheck; ensure no regressions in imports/types.</action>
  <verify>
    <automated>npm.cmd run lint && npm.cmd run typecheck</automated>
  </verify>
  <done>Advantages block is wired into the real page and validated with project baseline checks.</done>
</task>

</tasks>

<verification>
Run `npm.cmd run lint` and `npm.cmd run typecheck`. Manually check section in desktop/tablet/mobile viewports for heading structure and no overflow.
</verification>

<success_criteria>
Phase 2 planning artifacts are executable, concrete, and aligned to the user-approved narrowed scope (advantages block only).
</success_criteria>

<output>
After completion, create `.planning/phases/02-responsive-landing-and-proof-architecture/02-01-SUMMARY.md`.
</output>
