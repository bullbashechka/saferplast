---
phase: 02-responsive-landing-and-proof-architecture
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/landing/advantages-section-content.ts
  - src/features/landing/advantages-section.tsx
  - src/app/page.tsx
autonomous: true
requirements:
  - CONT-03
  - CONT-04
  - TRST-02
  - TRST-03
  - TRST-04
  - TRST-05
  - SITE-01
  - SITE-02
must_haves:
  truths:
    - "Блок преимуществ реализован как standalone semantic landing section с indexable text content."
    - "Секция адаптируется под desktop/tablet/mobile без horizontal overflow."
    - "Tailwind mapping следует visual contract из docs/preimushestva.md (light cards + one dark CTA card)."
  artifacts:
    - path: "src/features/landing/advantages-section-content.ts"
      provides: "Typed Russian content contract for headline/subheadline/cards"
      contains: "export const advantagesSectionContent"
    - path: "src/features/landing/advantages-section.tsx"
      provides: "Semantic and responsive advantages section component"
      contains: "<section"
    - path: "src/app/page.tsx"
      provides: "Landing composition includes advantages section"
      contains: "AdvantagesSection"
---

<objective>
Реализовать Phase 2 narrowed scope: только advantages block из `docs/preimushestva.md`, включая SEO-friendly semantic markup и mobile-first responsiveness.
</objective>

<execution_context>
@C:/Users/fm/Documents/Business/Saferplast_main/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/fm/Documents/Business/Saferplast_main/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-responsive-landing-and-proof-architecture/02-CONTEXT.md
@.planning/phases/02-responsive-landing-and-proof-architecture/02-RESEARCH.md
@docs/preimushestva.md
@src/app/page.tsx
@src/features/landing
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create typed content contract for the advantages block</name>
  <read_first>
    docs/preimushestva.md
    .planning/phases/02-responsive-landing-and-proof-architecture/02-CONTEXT.md
    src/features/landing/first-screen-content.ts
  </read_first>
  <files>src/features/landing/advantages-section-content.ts</files>
  <acceptance_criteria>
    File exports `advantagesSectionContent` с UTF-8 Russian text (no mojibake sequences like `Р`-prefixed broken glyphs).
    `title` exactly `Почему к нам обращаются`.
    `subtitle` exactly `Понятные условия, собственное производство и готовое решение`.
    Content includes 6 cards per `docs/preimushestva.md`, including one dark CTA card with label `Бесплатный замер`.
  </acceptance_criteria>
  <action>Создать `src/features/landing/advantages-section-content.ts` со strict typed structure: section `title`, `subtitle` и six card objects (`title`, `description`, `variant`, optional `href`). Заполнить card copy из `docs/preimushestva.md`, оставить CTA card как `variant: "dark"` и `href: "#lead-form"`, а все строки держать UTF-8-clean Russian text.</action>
  <verify>
    <automated>npm.cmd run typecheck</automated>
  </verify>
  <done>Content source централизован, типизирован и безопасен от encoding regressions.</done>
</task>

<task type="auto">
  <name>Task 2: Build semantic and responsive Tailwind section component</name>
  <read_first>
    docs/preimushestva.md
    src/features/landing/advantages-section-content.ts
    src/features/landing/hero-section.tsx
  </read_first>
  <files>src/features/landing/advantages-section.tsx</files>
  <acceptance_criteria>
    Root element - `<section>` с internal `<h2>` для title секции.
    Каждый card title рендерится как `<h3>`, а description - как plain text paragraph.
    Desktop layout использует multi-card grid с сохраненной visual hierarchy.
    Mobile layout становится one-column card stack без horizontal overflow.
    Tailwind classes отражают approved style direction: title 44px Sansation на desktop, body 16px Montserrat, card radius 20px, light gradient cards + one dark card.
  </acceptance_criteria>
  <action>Создать `src/features/landing/advantages-section.tsx`, который импортирует `advantagesSectionContent` и рендерит semantic SEO-friendly markup (`section > h2 + p + card list`). Реализовать responsive layout через Tailwind (`grid`/`flex` at breakpoints) без absolute coordinates. Замаппить visual tokens из `docs/preimushestva.md`: headline `text-[#004B62]`, light card gradient через arbitrary Tailwind gradient values, `rounded-[1.25rem]`, card paddings примерно `40px 30px`, и dark CTA card `bg-[#004B62] text-white` с clear interactive affordance.</action>
  <verify>
    <automated>npm.cmd run lint</automated>
  </verify>
  <done>Блок преимуществ production-usable, semantic и responsive, при этом соответствует approved design language.</done>
</task>

<task type="auto">
  <name>Task 3: Integrate the section into landing composition and verify behavior</name>
  <read_first>
    src/app/page.tsx
    src/features/landing/advantages-section.tsx
  </read_first>
  <files>src/app/page.tsx</files>
  <acceptance_criteria>
    `page.tsx` импортирует `AdvantagesSection` и рендерит его в landing flow после first-screen area.
    Existing Phase 1 header/hero composition остается intact.
    `npm.cmd run lint` и `npm.cmd run typecheck` pass.
  </acceptance_criteria>
  <action>Обновить `src/app/page.tsx`, чтобы включить `AdvantagesSection` в композицию страницы в месте, consistent с landing narrative flow, без изменения existing header/hero behavior. Запустить lint и typecheck; убедиться в отсутствии regressions в imports/types.</action>
  <verify>
    <automated>npm.cmd run lint && npm.cmd run typecheck</automated>
  </verify>
  <done>Блок преимуществ подключен к реальной странице и валидирован базовыми project checks.</done>
</task>

</tasks>

<verification>
Запустить `npm.cmd run lint` и `npm.cmd run typecheck`. Вручную проверить секцию на desktop/tablet/mobile viewport'ах на heading structure и отсутствие overflow.
</verification>

<success_criteria>
Phase 2 planning artifacts executable, concrete и aligned to user-approved narrowed scope (advantages block only).
</success_criteria>

<output>
После завершения создать `.planning/phases/02-responsive-landing-and-proof-architecture/02-01-SUMMARY.md`.
</output>