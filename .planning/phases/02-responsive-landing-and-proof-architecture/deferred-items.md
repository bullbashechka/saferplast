# Deferred Items

- 2026-03-25: Global `npm.cmd run lint` currently fails due to pre-existing ESLint violations in `.codex/get-shit-done/**/*.cjs` (CommonJS `require()` and unused vars). This is outside Phase 02-01 scope and not caused by current landing changes.
